<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630">
  <defs>
    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#0f172a"/>
      <stop offset="100%" stop-color="#1e293b"/>
    </linearGradient>
  </defs>
  <rect width="1200" height="630" fill="url(#bg)"/>

  <!-- Left side accent bar -->
  <rect x="0" y="0" width="12" height="630" fill="#38bdf8"/>

  <!-- Title -->
  <text x="60" y="220" font-family="Inter, sans-serif" font-size="64" font-weight="700" fill="#f1f5f9">
    icoda
  </text>

  <!-- Subtitle -->
  <text x="60" y="300" font-family="Inter, sans-serif" font-size="40" font-weight="500" fill="#38bdf8">
    Python/Django Developer
  </text>

  <!-- Tagline -->
  <text x="60" y="380" font-family="Inter, sans-serif" font-size="28" font-weight="400" fill="#cbd5e1">
    Building API-driven projects &amp; modern web apps
  </text>

  <!-- GitHub Pages URL -->
  <text x="60" y="480" font-family="Inter, sans-serif" font-size="28" font-weight="400" fill="#94a3b8">
    icoda.github.io
  </text>
</svg>